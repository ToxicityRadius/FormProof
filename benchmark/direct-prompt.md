Inspect the local web application served at {{URL}} and repair its automated accessibility barriers.

Make the smallest source-level change that fixes the barriers while preserving existing behavior. Run `{{REGRESSION_COMMAND}}` after editing. Do not read FormProof evidence or benchmark answer files, and do not claim WCAG conformance.

Finish with a concise summary of files changed, tests run, and anything requiring human review.
